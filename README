dpastecli is a small shell script to paste content from stdin to http://dpaste.com

key feautures:
  - small
  - reads from stdin
  - dpaste syntax supported
  - deletion of content possible

dependencies:
  - curl (http://curl.haxx.se/)

install:
  - put dpaste to /usr/local/bin
  - chmod +x /usr/local/bin/dpastecli

examples:
  - cat shellscript.sh | dpaste -s Bash
    (Syntax template for Bash)

  - echo "Hello World" | dpaste -o
    (Omit saving the cookie)

  - dpaste -d http://dpaste.com/<id>
    (Delete pasted content)
